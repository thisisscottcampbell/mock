// DEFINITION
//Returns an array of a given object's own enumerable property values (i.e., value of the object's keys), in the same order as that provided by a for...in loop.

const cats = {
  Garfield: lazy,
  CheshireCat: far-out,
  Tigger: lit
}

console.log(Object.values(object1));
//returns: [lazy, far-out, lit]