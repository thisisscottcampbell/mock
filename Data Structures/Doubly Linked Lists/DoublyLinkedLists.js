class Node {
	constructor(val) {
		this.val = val;
		this.next = null;
		this.next = null;
	}
}

class DoubleList {
	constructor(val) {
		const node = new Node(value);
		this.head = node;
		this.tail = this.head;
		value ? (this.length = 1) : (this.length = 0);
	}
}
